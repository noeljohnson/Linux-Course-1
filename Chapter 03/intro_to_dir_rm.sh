##GTG

mkdir -p new_dir/new_dir_2 #this creates a directory, and if parents dont exist creates them
# rmdir can be used to remove empty directories

rm -ir new_dir #removes new_dir recursively with an interactive prompt

# to remove without a prompt, use rm -rf file/folder


##TYJC
