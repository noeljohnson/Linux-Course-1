##GTG

file intro_to_cp.sh #returns the file type

cat intro_to_cp.sh # displays the whole file
cat -n intro_to_cp.sh #displays the whole file with lines numbers, use -b to count lines only with content

#use more, less commands when the file is more bulky, use less as it has more features

# to display the last lines of files you can use the tail command
tail intro_to_cp.sh #displays the last ten lines of the file
tail -n 3 intro_to_cp.sh # display the last three lines 
# use -f to see the last lines of a file that is continously being updated

##TYJC
