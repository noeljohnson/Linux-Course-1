##GTG

#history stores the last 1000 command typed in the terminal

history

# after a shell session is closed the history stored in memory is flushed to ~/.bash_history

#to flush the current sessions history use:
history -a

#to execute the last command in history
!!

#to execute 57th command in history
!57

sleep 5
##TYJC
